using UnityEngine;

public class EventManager_Bank : EventManager
{
    // This class can be used to override or extend EventManager for Bank-specific logic if needed.
    // For now, it inherits all logic from EventManager, but you can add Bank-specific methods here.
}